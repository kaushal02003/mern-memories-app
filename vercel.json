{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/build",
  "rewrites": [
    { "source": "/api/posts/:path*", "destination": "/api/posts" },
    { "source": "/api/posts", "destination": "/api/posts" }
  ]
}
